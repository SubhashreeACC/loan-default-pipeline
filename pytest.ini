# pytest.ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as requiring live Postgres/MLflow infrastructure
addopts = -v --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
